本文档描述了当前缺失的业务逻辑，需要补全和编写测试。

# 1、需要编写更加规范、更加接口化模块化的任务容器和管理设施

# 2、当前各种弹窗编辑界面没有真实的修改编辑功能，要将修改保存进数据库。同时任务能否编辑需要根据任务状态决定可以编辑的选项，比如说排程和之后的就不能随意编辑。但是在“阻塞态”栏下的任务点击进入的详情页面增加一个“取消阻塞”按钮，功能是将任务卡片返回“就绪”栏。

# 3、当前缺失日志系统，需要设计完整的日志系统和存储系统，包括提交日志、任务看板日志、编辑日志、操作日志、阻塞日志、各种审核结果等

# 4、当前拖拽功能只实现了基本限制，缺失正真的业务逻辑限制，具体要求：待处理、已分类、已排程之间可以向右拖拽，不能逆向，逆向只能对任务回滚，但是已排程和之后的就绪、执行、审核则是不允许拖拽。

# 5、看板更新逻辑：新任务入口是待处理，通过补全信息走向已排程，然后系统根据是否到了开始时间、是否被接单、是否被提交、阻塞申请情况自动更新到对应队列

# 6、需要生成虚拟的员工数据和企业数据供Agent使用，将这些数据以json形式保存在文件里。

# 7、需要在主标题栏的筛选右边添加ai工具按钮，样式是几条横线的按钮，按下后展开包含AGENT_TOOLS.md内7个命令的菜单。

# 8、全局搜索框功能不完善，需要完善功能，将ai工具的七个命令，日志输出功能也放入搜索框。

# 9、需要为Agent功能编写提示词md、skill和接入口，实现AGENT_TOOLS.md文档编写的agent功能。

# 10、在“验收中”栏下的任务点击进入的详情页面增加一个"ai审核”按钮，实现AGENT_TOOLS.md里内联工具写的功能，详情栏最下面的审核UI布局为：AI建议输出文本框（可滚动） 下放并排3个按钮：AI建议 驳回 同意

# 11、当前输入UI没有检测合规性和约束（ATA、时间、人员和ID），参考TASK_INFO.md增强实现各种约束检测

# 12、任务详情的编辑功能目前实现的非常简陋，结合任务 状态约束 和 TASK_INFO.md 实现风格统一的编辑弹窗

# 13、需要新增全局通知气泡，样式方形圆角，需要在合适的时候比如输入异常时弹出显示，具有动画。
