本文档描述了当前缺失的业务逻辑，需要补全和编写测试。

# 1、需要编写更加规范、更加接口化模块化的任务容器和管理设施

# 2、当前各种弹窗编辑界面只有界面但是没有真实的修改编辑功能，同时任务能否编辑需要根据任务状态决定可以编辑的选项，比如说排程和之后的就不能随意编辑

# 3、当前缺失日志系统，需要设计完整的日志系统和存储系统，包括提交日志、任务看板日志、编辑日志、操作日志、阻塞日志、各种审核结果等

# 4、当前拖拽功能只实现了基本限制，缺失正真的业务逻辑限制，具体要求：待处理、分类中、已排程之间可以向右拖拽，不能逆向，逆向只能对任务回滚，但是已排程和之后的就绪、执行、审核则是不允许拖拽

# 5、看板更新逻辑：新任务入口是待处理，通过补全信息走向已排程，然后系统根据是否到了开始时间、是否被接单、是否被提交、阻塞申请情况自动更新到对应队列

# 6、需要生成虚拟的员工数据和企业数据供Agent使用

# 7、需要在主标题栏的筛选右边添加ai工具按钮，按下后展开AGENT_TOOLS.md内8个命令菜单

# 8、全局搜索框功能不完善，需要完善功能

# 9、需要为Agent功能编写提示词md、skill和接入口

# 10、